{% set channel = operations[0].channel.publish -%}
import { {{ channel['x-response-definitions'] }} } from '../rabbitMQ/interfaces';

export default async (payload: {{ channel['x-response-definitions'] }}, operationId: string): Promise<void> => {
  // todo:  Handle the payload...
}
